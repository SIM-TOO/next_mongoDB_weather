export interface FormData {
    enteredEmail?: string;
    enteredPassword?: string;
    enteredConfirmPassword?: string;
}

export type User = {
    enteredEmail?: string;
    enteredPassword?: string;
};